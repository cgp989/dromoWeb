{% extends '::base.html.twig' %}

{% block body -%}
    <h2 class="sub-header">Editar Variables</h2>
    <div class="col-xs-12">
        {{ form_start(edit_form) }}
        <div class="row error-count">{{ form_errors(edit_form) }}</div>
        <div class="row">
            <div class="col-xs-4">{{ form_row(edit_form.porcCobroLocal) }}</div>
            <div class="col-xs-4 col-xs-offset-1">{{ form_row(edit_form.porcGanancia) }}</div>
        </div>
        {{ form_end(edit_form) }}
    </div>
{% endblock %}
