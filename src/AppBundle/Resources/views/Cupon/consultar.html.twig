{% extends '::base.html.twig' %}

{% block body -%}
    <h2 class="sub-header">Consultar c&oacute;digo cup&oacute;n</h2>
    <div class="col-xs-12">
        {{ form_start(consultar_form) }}
        <div class="row error-count">{{ form_errors(consultar_form) }}</div>
        <div class="row">
            <div class="col-xs-3">
                {{ form_row(consultar_form.codigo) }}
            </div>
        </div>
        <div class="row">
            <div class="col-xs-2 pull-right text-right">{{ form_row(consultar_form.buscar) }}</div>
        </div>
        {{ form_end(consultar_form) }}
    </div>
{% endblock %}

