{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pendientes de cobro de .... </h1>
    <div class="row btn-action-top">
        <div class="col-xs-4 pull-left">
            <a class="text-info" href=""><span class="glyphicon glyphicon-export"></span> Exportar a PDF</a>
        </div>
        <div class="col-xs-1 pull-right">
            <a title="volver" href="{{ path('cobros_pendientes_locales') }}"><span class="glyphicon glyphicon-circle-arrow-left"></span></a>
        </div>
    </div>
    <table class="table table-hover records_list">
        <thead>
            <tr>
                <th class="col-xs-3">Fecha</th>
                <th class="col-xs-6">Titulo</th>
                <th class="col-xs-2">Importe</th>
                <th class="col-xs-1">Cobrado</th>

            </tr>
        </thead>
        <tbody>
            {% for cupon in cuponesPendientes %}
                <tr>
                    <td>{{ cupon.fecha|date('d/m/Y') }}</td>
                    <td>{{ cupon.titulo }}</td>
                    <td>{{ cupon.precio}}</td>
                    <td>
                        <a title="cobrado" href="{{ path('cobro_local', { 'id': cupon.idLocal, 'idCupon': cupon.id}) }}"><span class="glyphicon glyphicon-ok"></span></a> 
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
