{% extends '::base.html.twig' %}

{% block body -%}
    <h2 class="sub-header">Ver promoci&oacute;n</h2>

    <table class="table table-show">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ entity.titulo }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ entity.precio }}</td>
            </tr>
            <tr>
                <th>Estamoderada</th>
                <td>{{ entity.estaModerada }}</td>
            </tr>
            <tr>
                <th>Puntajepremio</th>
                <td>{{ entity.puntajePremio }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('promocion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('promocion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
