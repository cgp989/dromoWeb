{% extends '::base.html.twig' %}

{% block body -%}
    <h3 class="sub-header">Promociones</h3>
    <a title="nueva" class="new-entity pull-right" href="{{ path('promocion_new') }}">
        <span class="glyphicon glyphicon-plus-sign">
    </a>
    <table class="table table-hover records_list">
        <thead>
            <tr>
                <th>Tipo</th>
                <th>Titulo</th>
                <th>Precio</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td class="">{{ entity.tipoPromocion }}</td>
                <td>{{ entity.titulo }}</td>
                <td>${{ entity.precio }}</td>
                <td>{{ entity.estadoPromocion }}</td>
                <td>
                    <a title="ver" href="{{ path('promocion_show', { 'id': entity.id }) }}"><span class="glyphicon glyphicon-eye-open"></span></a>
                    &nbsp;&nbsp;&nbsp;
                    <a title="editar" href="{{ path('promocion_edit', { 'id': entity.id }) }}"><span class="glyphicon glyphicon-edit"></span></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% endblock %}
