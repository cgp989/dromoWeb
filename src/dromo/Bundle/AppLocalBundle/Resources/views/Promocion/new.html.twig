{% extends '::base.html.twig' %}
{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}
{% block body -%}
    <h1>Promocion creation</h1>

    <div class="col-xs-12">
        {{ form_start(form) }}
        {{ form_end(form) }}
    </div>
    

        <ul class="record_actions">
    <li>
        <a href="{{ path('promocion') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
